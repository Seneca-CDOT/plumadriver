version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:lts

    working_directory: ~/plumadriver

    steps:
      - checkout

      - run:
          name: Install dependencies
          command: npm install

      - run:
          name: Run tests
          command: npm test
